{
  "dependencies": {
    "firebase": "^7.21.0"
  },
  "devDependencies": {
    "elm-format": "^0.8.4",
    "elm-hot": "^1.1.5",
    "elm-test": "^0.19.1-revision4",
    "node-elm-compiler": "^5.0.5",
    "parcel-bundler": "^1.12.4",
    "typescript": "^4.0.5"
  },
  "scripts": {
    "dev": "rm -rf dist && parcel --port 3000 src/index.html",
    "build": "rm -rf dist && parcel build src/index.html",
    "format": "elm-format src",
    "test": "elm-test"
  }
}
